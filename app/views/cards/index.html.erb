  <h1> Список карточек </h1>
  <ul>
    <% @cards.each do |card| %>
      <li><%= card.inspect %></li>
    <% end %>
  </ul>
  <%= link_to 'Новая', new_card_path  %>

  <p>Количество карточек: <%= @cards.size %></p>
